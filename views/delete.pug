extends layout 

block content
  h1= title 

  p Are you sure you want to delete #[span #{book.title}]?

  form(action="/books/" + book.id + "/delete", method="post")
    a(href="/books/" + book.id class="error") No! Take me back! 
    | 
    input(type="submit", value = "Yes, I'm sure")



